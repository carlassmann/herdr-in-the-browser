diff --git a/dist/ghostty-web.js b/dist/ghostty-web.js
index a4ca427b95288e70c782ce303013cd644893dcec..4abce4567147f8da06bb0b23f1d60d9cd6b974fc 100644
--- a/dist/ghostty-web.js
+++ b/dist/ghostty-web.js
@@ -88,7 +88,7 @@ class V {
     if (g !== 0)
       throw new Error(`Failed to create key event: ${g}`);
     const E = new DataView(this.exports.memory.buffer), C = E.getUint32(B, !0);
-    if (this.exports.ghostty_wasm_free_opaque(B), this.exports.ghostty_key_event_set_action(C, A.action), this.exports.ghostty_key_event_set_key(C, A.key), this.exports.ghostty_key_event_set_mods(C, A.mods), A.utf8) {
+    if (this.exports.ghostty_wasm_free_opaque(B), this.exports.ghostty_key_event_set_action(C, A.action), this.exports.ghostty_key_event_set_key(C, A.key), this.exports.ghostty_key_event_set_mods(C, A.mods), A.consumedMods !== void 0 && this.exports.ghostty_key_event_set_consumed_mods(C, A.consumedMods), A.unshiftedCodepoint !== void 0 && this.exports.ghostty_key_event_set_unshifted_codepoint(C, A.unshiftedCodepoint), A.utf8) {
       const M = new TextEncoder().encode(A.utf8), a = this.exports.ghostty_wasm_alloc_u8_array(M.length);
       new Uint8Array(this.exports.memory.buffer).set(M, a), this.exports.ghostty_key_event_set_utf8(C, a, M.length), this.exports.ghostty_wasm_free_u8_array(a, M.length);
     }
@@ -1338,7 +1338,7 @@ class $ {
     const g = A.getContext("2d", { alpha: !0 });
     if (!g)
       throw new Error("Failed to get 2D rendering context");
-    this.ctx = g, this.fontSize = B.fontSize ?? 15, this.fontFamily = B.fontFamily ?? "monospace", this.cursorStyle = B.cursorStyle ?? "block", this.cursorBlink = B.cursorBlink ?? !1, this.theme = { ...f, ...B.theme }, this.devicePixelRatio = B.devicePixelRatio ?? window.devicePixelRatio ?? 1, this.palette = [
+    this.ctx = g, this.fontSize = B.fontSize ?? 15, this.fontFamily = B.fontFamily ?? "monospace", this.cellWidthAdjustment = B.cellWidthAdjustment, this.cellHeightAdjustment = B.cellHeightAdjustment, this.cursorStyle = B.cursorStyle ?? "block", this.cursorBlink = B.cursorBlink ?? !1, this.theme = { ...f, ...B.theme }, this.devicePixelRatio = B.devicePixelRatio ?? window.devicePixelRatio ?? 1, this.palette = [
       this.theme.black,
       this.theme.red,
       this.theme.green,
@@ -1363,8 +1363,40 @@ class $ {
   measureFont() {
     const B = document.createElement("canvas").getContext("2d");
     B.font = `${this.fontSize}px ${this.fontFamily}`;
-    const g = B.measureText("M"), E = Math.ceil(g.width), C = g.actualBoundingBoxAscent || this.fontSize * 0.8, I = g.actualBoundingBoxDescent || this.fontSize * 0.2, D = Math.ceil(C + I) + 2, i = Math.ceil(C) + 1;
-    return { width: E, height: D, baseline: i };
+    const g = B.measureText("M"), E = Math.ceil(g.width), C = g.actualBoundingBoxAscent || this.fontSize * 0.8, I = g.actualBoundingBoxDescent || this.fontSize * 0.2, D = Math.ceil(C + I) + 2, i = Math.ceil(C) + 1, w = this.adjustMetric(E, this.cellWidthAdjustment), s = this.adjustMetric(D, this.cellHeightAdjustment);
+    return this.spriteGlyphBox = void 0, { width: w, height: s, baseline: i + Math.round((s - D) / 2) };
+  }
+  /**
+   * Ghostty rasterizes box drawing, block elements, legacy computing and
+   * Powerline glyphs itself so they always fill the cell exactly. Emulate
+   * that by stretching the font glyph to the cell, using the full block as
+   * the reference for the font's glyph box.
+   */
+  isSpriteGlyph(A) {
+    return A >= 9472 && A <= 9631 || A >= 129024 && A <= 129279 || A >= 57520 && A <= 57559;
+  }
+  measureSpriteGlyphBox() {
+    if (this.spriteGlyphBox !== void 0)
+      return this.spriteGlyphBox;
+    const A = document.createElement("canvas").getContext("2d");
+    A.font = `${this.fontSize}px ${this.fontFamily}`;
+    const B = A.measureText("\u2588"), g = B.actualBoundingBoxAscent + B.actualBoundingBoxDescent, E = B.actualBoundingBoxLeft + B.actualBoundingBoxRight;
+    return this.spriteGlyphBox = g > 0 && E > 0 ? { width: E, height: g, ascent: B.actualBoundingBoxAscent, left: B.actualBoundingBoxLeft } : null;
+  }
+  fillSpriteGlyph(A, B, g, E) {
+    // Measured glyph boxes run a hair wider than the rasterized shape, which
+    // leaves seams between adjacent cells. Overdraw by a pixel and clip.
+    const C = this.measureSpriteGlyphBox();
+    if (!C)
+      return !1;
+    const I = 1, D = (E + 2 * I) / C.width, i = (this.metrics.height + 2 * I) / C.height;
+    return this.ctx.save(), this.ctx.beginPath(), this.ctx.rect(B, g, E, this.metrics.height), this.ctx.clip(), this.ctx.translate(B - I + C.left * D, g - I + C.ascent * i), this.ctx.scale(D, i), this.ctx.fillText(A, 0, 0), this.ctx.restore(), !0;
+  }
+  adjustMetric(A, B) {
+    if (!B)
+      return A;
+    const g = B.unit === "percent" ? A * B.value / 100 : B.value;
+    return Math.max(1, Math.round(A + g));
   }
   /**
    * Remeasure font metrics (call after font loads or changes)
@@ -1535,7 +1567,7 @@ class $ {
     A.flags & e.FAINT && (this.ctx.globalAlpha = 0.5);
     const w = E, s = C + this.metrics.baseline;
     let N;
-    if (A.grapheme_len > 0 && ((k = this.currentBuffer) != null && k.getGraphemeString) ? N = this.currentBuffer.getGraphemeString(g, B) : N = String.fromCodePoint(A.codepoint || 32), this.ctx.fillText(N, w, s), A.flags & e.FAINT && (this.ctx.globalAlpha = 1), A.flags & e.UNDERLINE) {
+    if (A.grapheme_len > 0 && ((k = this.currentBuffer) != null && k.getGraphemeString) ? N = this.currentBuffer.getGraphemeString(g, B) : N = String.fromCodePoint(A.codepoint || 32), A.grapheme_len === 0 && this.isSpriteGlyph(A.codepoint) && this.fillSpriteGlyph(N, E, C, I) || this.ctx.fillText(N, w, s), A.flags & e.FAINT && (this.ctx.globalAlpha = 1), A.flags & e.UNDERLINE) {
       const M = C + this.metrics.baseline + 2;
       this.ctx.strokeStyle = this.ctx.fillStyle, this.ctx.lineWidth = 1, this.ctx.beginPath(), this.ctx.moveTo(E, M), this.ctx.lineTo(E + I, M), this.ctx.stroke();
     }
@@ -2136,7 +2168,7 @@ class IA {
       }
     }, this.handleMouseLeave = () => {
       var g, E;
-      this.renderer && this.wasmTerm && ((this.renderer.hoveredHyperlinkId || 0) > 0 && this.renderer.setHoveredHyperlinkId(0), this.renderer.setHoveredLinkRange(null)), this.currentHoveredLink && ((E = (g = this.currentHoveredLink).hover) == null || E.call(g, !1), this.currentHoveredLink = void 0, this.element && (this.element.style.cursor = "text"));
+      this.renderer && this.wasmTerm && ((this.renderer.hoveredHyperlinkId || 0) > 0 && this.renderer.setHoveredHyperlinkId(0), this.renderer.setHoveredLinkRange(null)), this.currentHoveredLink && ((E = (g = this.currentHoveredLink).hover) == null || E.call(g, !1), this.currentHoveredLink = void 0, this.element && (this.element.style.cursor = ""));
     }, this.handleClick = async (g) => {
       if (!this.canvas || !this.renderer || !this.linkDetector || !this.wasmTerm)
         return;
@@ -2203,6 +2235,8 @@ class IA {
       scrollback: A.scrollback ?? 1e4,
       fontSize: A.fontSize ?? 15,
       fontFamily: A.fontFamily ?? "monospace",
+      cellWidthAdjustment: A.cellWidthAdjustment,
+      cellHeightAdjustment: A.cellHeightAdjustment,
       allowTransparency: A.allowTransparency ?? !1,
       convertEol: A.convertEol ?? !1,
       disableStdin: A.disableStdin ?? !1,
@@ -2338,6 +2372,8 @@ class IA {
       }), this.renderer = new $(this.canvas, {
         fontSize: this.options.fontSize,
         fontFamily: this.options.fontFamily,
+        cellWidthAdjustment: this.options.cellWidthAdjustment,
+        cellHeightAdjustment: this.options.cellHeightAdjustment,
         cursorStyle: this.options.cursorStyle,
         cursorBlink: this.options.cursorBlink,
         theme: this.options.theme
@@ -2712,7 +2748,7 @@ class IA {
       k = N + C;
     this.linkDetector.getLinkAt(g, k).then((h) => {
       var G, U, t, c;
-      if (h !== this.currentHoveredLink && ((U = (G = this.currentHoveredLink) == null ? void 0 : G.hover) == null || U.call(G, !1), this.currentHoveredLink = h, (t = h == null ? void 0 : h.hover) == null || t.call(h, !0), this.element && (this.element.style.cursor = h ? "pointer" : "text"), this.renderer))
+      if (h !== this.currentHoveredLink && ((U = (G = this.currentHoveredLink) == null ? void 0 : G.hover) == null || U.call(G, !1), this.currentHoveredLink = h, (t = h == null ? void 0 : h.hover) == null || t.call(h, !0), this.element && (this.element.style.cursor = h ? "pointer" : ""), this.renderer))
         if (h) {
           const F = ((c = this.wasmTerm) == null ? void 0 : c.getScrollbackLength()) || 0, S = this.getViewportY(), x = Math.max(0, Math.floor(S)), p = h.range.start.y - F + x, T = h.range.end.y - F + x;
           p < this.rows && T >= 0 ? this.renderer.setHoveredLinkRange({
@@ -2958,6 +2994,10 @@ export {
   AA as SelectionManager,
   IA as Terminal,
   _ as UrlRegexProvider,
+  o as Key,
+  b as KeyAction,
+  y as Mods,
+  u as keyCodeMap,
   CA as getGhostty,
   oA as init
 };
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 19c99f9caf2f6b006496c0db3b32ea4ea96496a6..49f247049163186a8389a7d371a8b81a0ed9a6ba 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -3,6 +3,8 @@ export declare class CanvasRenderer {
     private ctx;
     private fontSize;
     private fontFamily;
+    private cellWidthAdjustment;
+    private cellHeightAdjustment;
     private cursorStyle;
     private cursorBlink;
     private theme;
@@ -891,6 +893,8 @@ export declare interface ITerminalOptions {
     scrollback?: number;
     fontSize?: number;
     fontFamily?: string;
+    cellWidthAdjustment?: MetricAdjustment;
+    cellHeightAdjustment?: MetricAdjustment;
     allowTransparency?: boolean;
     convertEol?: boolean;
     disableStdin?: boolean;
@@ -1261,12 +1265,19 @@ export declare class OSC8LinkProvider implements ILinkProvider {
 export declare interface RendererOptions {
     fontSize?: number;
     fontFamily?: string;
+    cellWidthAdjustment?: MetricAdjustment;
+    cellHeightAdjustment?: MetricAdjustment;
     cursorStyle?: 'block' | 'underline' | 'bar';
     cursorBlink?: boolean;
     theme?: ITheme;
     devicePixelRatio?: number;
 }
 
+export declare interface MetricAdjustment {
+    value: number;
+    unit: 'pixels' | 'percent';
+}
+
 /**
  * Colors from RenderState (12 bytes packed)
  */
@@ -1460,7 +1471,7 @@ export declare class Terminal implements ITerminalCore {
     readonly buffer: IBufferNamespace;
     readonly unicode: IUnicodeVersionProvider;
     readonly options: Required<ITerminalOptions>;
-    private ghostty?;
+    readonly ghostty: Ghostty;
     wasmTerm?: GhosttyTerminal;
     renderer?: CanvasRenderer;
     private inputHandler?;
@@ -1840,3 +1851,6 @@ export declare class UrlRegexProvider implements ILinkProvider {
 }
 
 export { }
+
+/** Browser KeyboardEvent.code to Ghostty key mapping used by InputHandler. */
+export declare const keyCodeMap: Record<string, Key>;
